# 🤖 AI-парсинг запросов для умного поиска

## 📋 Обзор

Реализован AI-парсер запросов с использованием DeepSeek API для значительного улучшения качества поиска в проекте "Второй мозг". AI-парсер решает проблемы ограниченности регулярных выражений и обеспечивает семантическое понимание запросов пользователя.

## 🚫 Проблемы обычного парсинга

### Ограничения регулярных выражений:
1. **Нет понимания синонимов** - "авто" ≠ "машина" ≠ "автомобиль"
2. **Ручная нормализация** - словарь быстро устаревает
3. **Нет контекста** - "работа" может быть место или деятельность
4. **Ограниченные паттерны** - много запросов не распознаются
5. **Нет понимания намерений** - одинаковое обращение к разным намерениям

## ✨ Преимущества AI-парсинга

### Новые возможности:
1. **Понимание синонимов** - авто, машина, автомобиль → object
2. **Умная нормализация** - васе, васю, васи → вася
3. **Контекстный анализ** - работа в офисе vs работа над проектом
4. **Понимание намерений** - покажи, расскажи, что знаешь → search
5. **Семантический поиск** - что с Васей = расскажи о Васе

## 🏗️ Архитектура

### Новый модуль: `ai_query_parser.py`

```python
class AIQueryParser:
    """AI парсер для умного анализа запросов пользователя"""
    
    def __init__(self, api_key: str, base_url: str = "https://api.deepseek.com")
    async def parse_query(self, query: str) -> Dict
    def extract_search_terms(self, parsed_result: Dict) -> Dict[str, List[str]]
    def get_search_strategy(self, parsed_result: Dict) -> str
    def get_intent(self, parsed_result: Dict) -> str
```

### Интеграция в `smart_search.py`:

```python
class SmartSearchEngine:
    def __init__(self, db_manager: DatabaseManager, ai_api_key: str = None):
        self.ai_parser = AIQueryParser(ai_api_key) if ai_api_key else None
    
    async def parse_query_ai(self, query: str) -> Dict[str, List[str]]
    async def search_comprehensive(self, user_id: int, query: str) -> Dict
```

## 🎯 Функциональность

### AI-анализ запросов:
- **Извлечение сущностей** с определением типов
- **Нормализация слов** к базовой форме
- **Поиск синонимов** и связанных понятий
- **Определение намерений** пользователя
- **Контекстный анализ** для точной категоризации

### Fallback система:
- При ошибке AI → автоматический переход на обычный парсинг
- При отсутствии API ключа → использование fallback парсинга
- Полная совместимость с существующим кодом

## 📊 Примеры работы

### Сравнение парсинга:

```
Запрос: "расскажи о Васе"

🔧 Обычный парсинг:
  person: вася
  general: вася

🤖 AI-парсинг:
  person: вася
  intent: search
  strategy: semantic
  confidence: 0.9
```

```
Запрос: "что с автомобилем"

🔧 Обычный парсинг:
  general: автомобилем

🤖 AI-парсинг:
  object: автомобиль, машина, авто
  synonyms: [машина, авто, транспорт]
  intent: search
  strategy: fuzzy
```

## 🧪 Тестирование

### Тестовые файлы:
- `test_ai_parsing.py` - тестирование AI-парсера
- `demo_ai_parsing.py` - демонстрация различий
- `test_smart_tell.py` - интеграционное тестирование

### Запуск тестов:
```bash
python3 test_ai_parsing.py      # Тест AI-парсера
python3 demo_ai_parsing.py      # Демонстрация
python3 test_smart_tell.py      # Полное тестирование
```

## 🔧 Конфигурация

### Переменные окружения:
```bash
DEEPSEEK_API_KEY=your_deepseek_api_key_here
```

### Инициализация:
```python
# С AI-парсингом
search_engine = SmartSearchEngine(db, DEEPSEEK_API_KEY)

# Без AI (fallback)
search_engine = SmartSearchEngine(db)
```

## 📈 Результаты

### Улучшения качества поиска:
- ✅ **Понимание синонимов** - находит связанные записи
- ✅ **Автоматическая нормализация** - работает с любыми словами
- ✅ **Контекстный анализ** - точная категоризация
- ✅ **Семантический поиск** - понимает смысл запросов
- ✅ **Fallback система** - надежность при ошибках

### Совместимость:
- ✅ Полная обратная совместимость
- ✅ Работает без API ключа (fallback)
- ✅ Интегрирован в существующую архитектуру
- ✅ Не нарушает работу бота

## 🚀 Готово к использованию

AI-парсинг полностью интегрирован и готов к использованию:

1. **Установите DEEPSEEK_API_KEY** в переменные окружения
2. **Перезапустите бота** - AI-парсинг активируется автоматически
3. **Наслаждайтесь улучшенным поиском** с пониманием синонимов и контекста

### Примеры улучшенных запросов:
- "что с автомобилем" → найдет записи о машине, авто, транспорте
- "расскажи про здоровье и медицину" → найдет записи о лечении, врачах, болезнях
- "покажи финансы и деньги" → найдет записи о покупках, тратах, бюджете

AI-парсинг значительно повышает качество поиска и делает взаимодействие с ботом более естественным! 🎉
